# v0.7 README Refresh Plan

## Scope

Align the root README with the v0.7 product surface while keeping it concise,
evergreen, and useful to prospective users. Release-specific migration detail
stays in `RELEASE_NOTES.md` and the upgrading guide.

## Recommended Changes

1. Update the feature list to name the unified Vault Explorer, inline edit
   review, file revision history and activity rollback, durable workflow and
   System Activity visibility, and the stable web retrieval capabilities.
2. Remove direct vault-file browsing from the Roadmap because it ships in v0.7.
3. Tighten generic or potentially overbroad claims, especially “all major LLM
   providers,” and replace marketing-style UI wording with concrete behavior.
4. Add a direct Release Notes link beside the installation, build, security,
   and upgrading documentation.
5. Preserve the concise opening explanation of vaults and workspaces; mention
   the Vault Explorer there only if the feature list does not make its role
   sufficiently clear.
6. Correct the adjacent `workspace.exists` contract in
   `docs/architecture/authoring-engine.md`: it means the configured workspace
   directory currently exists, not merely that a path is set.

## Affected Areas

- `README.md`
- `docs/architecture/authoring-engine.md`

No settings, secrets, API payloads, or persisted runtime data change.

## Validation

- Check all README links and anchors.
- Search the README and current-contract docs for stale claims that vault
  browsing is future work or that `workspace.exists` means only “path set.”
- Review the rendered Markdown for concise line lengths and balanced feature
  emphasis.
- Run `git diff --check`; no runtime validation scenario is needed for
  documentation-only edits.

## Next Phase

Proceed through Feature Development for the documentation edits, then Cleanup
Before Merge for a final contract and brevity review.

## Implementation Status

Implemented:

- Added a concise v0.7 callout near the top of the README.
- Reframed the introduction around a familiar AI chat interface and
  AssistantMD's integrated, vault-native Markdown editing workflow.
- Explained that composability adapts areas of a vault to different kinds of
  work rather than requiring users to create and manage separate agents.
- Updated the evergreen feature list and removed shipped work from the Roadmap.
- Raised experimental OpenAI OAuth near the top of the feature list while
  keeping general model connectivity out of the opening pitch.
- Added the Release Notes documentation link.
- Corrected the `workspace.exists` authoring contract.

Remaining:

- None. Link, stale-claim, Markdown, and diff checks are complete.
