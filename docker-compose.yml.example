# Default Docker Compose configuration that pulls the published GHCR image.
# Copy this file to docker-compose.yml and run: docker compose up -d

services:
  assistant:
    image: ghcr.io/dodgybadger/assistantmd:latest
    container_name: assistantMD
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"    # FastAPI - localhost only
    volumes:
      - /absolute/path/to/your/vaults:/app/data   # Pre-create path on the left of the colon & ensure owned by your user
      - ./system:/app/system                      # Pre-create path on the left of the colon & ensure owned by your user
    environment:
      - TZ=UTC
      - API_PORT=8000
    
    # Resource limits for better container security
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M
          cpus: '0.5'
    
    # Security options
    security_opt:
      - no-new-privileges:true
    
#   # Health check
#    healthcheck:
#      test: ["CMD", "curl", "-f", "http://localhost:${API_PORT:-8000}/api/health"]
#      interval: 120s
#      timeout: 10s
#      retries: 3
#      start_period: 40s

# Optional self-hosted Piston code execution sandbox
# Uncomment to run Piston locally instead of the public endpoint.
# piston:
#   image: ghcr.io/engineer-man/piston:latest
#   container_name: piston
#   restart: unless-stopped
#   # Disable outbound network from Piston containers (recommended)
#   network_mode: "none"
#   # Piston listens on 2000 by default; the app talks to it via the internal service name.
#   # Keep it internal-only; expose a host port only if you explicitly need it.
#   environment: []
#   # Optional: tighten runtime further
#   read_only: true
#   tmpfs:
#     - /tmp
#   deploy:
#     resources:
#       limits:
#         memory: 1G
#         cpus: "1.0"
